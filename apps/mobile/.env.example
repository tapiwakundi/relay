# Generated locally by `pnpm setup:env` (LAN IP so the phone can reach the API).
# Release/TestFlight builds ignore LAN/localhost and use https://relay-api-rsck.onrender.com.
EXPO_PUBLIC_API_URL=http://127.0.0.1:3001

# Optional Expo services
EXPO_ACCESS_TOKEN=
