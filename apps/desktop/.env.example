# Local `pnpm dev` loads .env.local (gitignored; created on first run if missing).
RELAY_API_URL=http://localhost:3001

# Packaged builds load .env.prod instead:
# RELAY_API_URL=https://relay-api-rsck.onrender.com
